# Hermes Volta — pin ranges for reproducible installs outside Hermes-Agent.
# Optional: omit fastapi stack if you only run sim/ CLI via install_deps.sh.
numpy>=1.24,<2
matplotlib>=3.7,<4
PySpice>=1.5.5,<2
skidl>=1.0,<2
requests>=2.28,<3
fastapi>=0.109,<1
uvicorn[standard]>=0.27,<1
python-multipart>=0.0.9
pydantic>=2.5,<3
